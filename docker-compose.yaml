
services:
  main-script:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./artifacts: /app/atrifacts

#    command: python3 main.py

  training-script:
    build:
      context: .
      dockerfile: Dockerfile.training
#    command: python3 training.py

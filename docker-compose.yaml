
services:
  main-script:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./artifacts:/app/artifacts
      - ./encoded_to_sensor_mapping.json:/app/encoded_to_sensor_mapping.json

#    command: python3 main.py

  training-script:
    build:
      context: .
      dockerfile: Dockerfile.training
    volumes:
      - ./artifacts/model_trainer:/app/artifacts/model_trainer
      - ./artifacts/model_evaluation:/app/artifacts/model_evaluation
#    command: python3 training.py
